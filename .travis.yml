sudo: required
language: java
jdk: oraclejdk8
install: true
dist: trusty

script:
    - cd acc-discovery
    - mvn clean install
    
    - cd ../acc-config
    - mvn clean install
    
    - cd ../acc-gateway
    - mvn clean install
    
    - cd ../acc-car
    - mvn clean install

    - cd ../acc-customer
    - mvn clean install
