spring:
  application:
    name: acc-car
  cloud:
    config:
      username: admin
      password: admin@123
      uri: http://localhost:8888
      
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/