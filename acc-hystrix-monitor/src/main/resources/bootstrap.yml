spring:
  application:
    name: acc-hystrix-monitor
  cloud:
    config:
      uri: http://localhost:8888
      
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
