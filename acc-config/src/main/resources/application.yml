server:
  port: 8888
spring:  profiles:    active:    - native  security:    user:      name: admin      password: admin@123  cloud:    config:      server:        native:          search-locations:          - classpath:/config
eureka:  client:    fetch-registry: true
    register-with-eureka: true    service-url:      defaultZone: http://localhost:8761/eureka/
      management:  endpoints:    web:      exposure:        include:        - '*'